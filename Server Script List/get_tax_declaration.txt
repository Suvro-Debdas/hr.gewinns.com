frappe.response["message"]=frappe.db.sql("SELECT e.employee,e.employee_name,a.department,a.payroll_period,a.total_declared_amount,a.total_exemption_amount,c.exemption_sub_category,c.exemption_category,c.max_amount,c.amount FROM `tabEmployee Tax Exemption Declaration` as a INNER JOIN `tabEmployee Tax Exemption Declaration Category` as c ON a.name = c.parent INNER JOIN `tabEmployee` as e ON a.employee = e.employee WHERE e.user_id = %s",(frappe.session.user), as_dict = True)