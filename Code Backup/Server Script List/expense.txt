frappe.response["message"]=frappe.db.sql("SELECT c.employee_name,a.department,a.total_claimed_amount,a.total_amount_reimbursed,a.mode_of_payment,a.expense_approver,a.status,e.description,e.amount,e.cost_center,e.expense_date,e.expense_type,e.sanctioned_amount,a.posting_date,a.name FROM `tabExpense Claim` as a  INNER JOIN `tabExpense Claim Detail` as e ON a.name=e.parent INNER JOIN `tabEmployee` as c ON a.employee=c.employee  WHERE c.user_id = %s",(frappe.session.user), as_dict = True)